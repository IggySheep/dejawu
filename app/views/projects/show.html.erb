


<h1><%= @project.title %></h1>

<h3>Beschreibung: </h3>
<p><%= @project.description %></p>

<h3>Interne Projektnummer: </h3>
  <p> <%= @project.noInt %></p>

  <h3>Externe Projektnummer: </h3>
   <p> <%= @project.noExt %></p>

   <h3>Zu erledigende Arbeiten </h3>
    <p> <%= @project.workTodo %></p>

	<%= link_to "Editieren", edit_project_path %>
	<%= link_to "löschen", project_path(@project) , method: :delete, data: { confirm: "Bist Du sicher?"} %>
	<%= link_to "Zurueck" , root_path%>
